import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RegisteredpassPage } from './registeredpass';

@NgModule({
  declarations: [
    RegisteredpassPage,
  ],
  imports: [
    IonicPageModule.forChild(RegisteredpassPage),
  ],
})
export class RegisteredpassPageModule {}
